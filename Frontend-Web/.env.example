# Backend API base URL (no trailing slash).
# Local dev with Vite proxy to a local backend: leave empty (same-origin /api/* → localhost:3000)
# Local dev against live Render backend:
# VITE_API_BASE_URL=https://verve-dental-api.onrender.com
VITE_API_BASE_URL=https://verve-dental-api.onrender.com
