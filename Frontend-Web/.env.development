# Optional override for local development.
# Leave empty to use the Vite dev proxy (/api/* → http://localhost:3000).
# Uncomment to hit the live Render backend while running `npm run dev`:
# VITE_API_BASE_URL=https://verve-dental-api.onrender.com
