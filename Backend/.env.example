NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database (PostgreSQL) — use AWS Secrets Manager in deployed environments
DATABASE_URL=postgresql://user:password@localhost:5432/verve_dev

# Redis — caching layer
REDIS_URL=redis://localhost:6379
